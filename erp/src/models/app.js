import request from '../utils/request';
import {
  getCustomerList,
  getCustomerById
} from '../services/cashier'

const App = {
  namespace: "app",

  state: {
    app:"app"
  },
  reducers: {
    
  },
  effects: {
    
  },
  subscriptions: {
    
  }
};

export default App;
